@inject PageData PageData

<MudButton>
    <MudNavLink Href="" Style="@("color: #" + (PageData.WebPage == WebPage.About ? "0f0f" : "ffff"))">About</MudNavLink>
</MudButton>
|
<MudButton>
    <MudNavLink Href="/Events" Style="@("color: #" + (PageData.WebPage == WebPage.Events ? "0f0f" : "ffff"))">Events</MudNavLink>
</MudButton>
|
<MudButton>
    <MudNavLink Href="/Resources" Style="@("color: #" + (PageData.WebPage == WebPage.Resources ? "0f0f" : "ffff"))">Resources</MudNavLink>
</MudButton>

@code {

    public void Refresh()
    {
        StateHasChanged();
    }

}